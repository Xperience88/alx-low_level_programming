#include "main.h"

/**
 * mul - multiplies two integers
 * @a: the first number to multiply
 * @b: the second number to multiply with the first
 * Return: the results of multiplication between a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
